server:
  port: 8088
  servlet:
    contextPath: /
spring:
  profiles:
    active: dev, test
  cloud:
    config:
      server:
        git:
          uri: https://gitlab.com/Nikitos1997/jm_social_project_micro.git
          repos:
            dev:
              pattern: dev/*
              uri:
            prod:
              pattern: prod/*
              uri:
            test:
              pattern: test/*
              uri: