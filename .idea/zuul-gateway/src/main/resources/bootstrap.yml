spring:
  application:
    name: gateway

server:
  port: 8080
 
#eureka:
#  client:
#    serviceUrl:
#      defaultZone:

zuul:
  routes:
    feign-demo:
      path: /service/**
      serviceId: feign-demo
    eureka-client: 
      path: /api/**
      serviceId: eureka-client
      stripPrefix: false

#hystrix:
#  command:
#    default:
#      execution:
#        isolation:
#          thread:
#            timeoutInMilliseconds: 60000